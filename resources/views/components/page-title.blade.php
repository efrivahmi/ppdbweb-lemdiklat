@props([
    "title" => "",
    "subtitle" => "",
])
<div class="mb-6">
     <h1 class="text-3xl font-bold text-gray-900 capitalize">{{ $title }}</h1>
     <p class="text-gray-600 mt-1">{{ $subtitle }}</p>
 </div>
